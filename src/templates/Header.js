const Header = () => {
    const view = `
        <header>
            <a class="header__logo"></a>
            <a class="header__about">About</a>
        </header>
    `;

    return view;
}

export default Header;